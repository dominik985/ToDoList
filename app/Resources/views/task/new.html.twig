{% extends 'base.html.twig' %}

{% block body %}

    <h2 class="page-header">Dodaj zadanie</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" class="btn btn-success"/>
        <a href="{{ path('task_index') }}" class="btn btn-default">Wyświetl wszystkie</a>
    {{ form_end(form) }}
    <br><br><br>
{% endblock %}
